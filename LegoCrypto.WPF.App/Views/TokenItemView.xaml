<UserControl x:Class="LegoCrypto.WPF.App.Views.TokenItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LegoCrypto.WPF.App.ViewModels"
             xmlns:local="clr-namespace:LegoCrypto.WPF.App.Views"
             mc:Ignorable="d" >
    <Grid>
        <Grid.DataContext>
            <vm:TokenItemViewModel />
        </Grid.DataContext>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="Type" />
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding TokenTypes}" IsSynchronizedWithCurrentItem="True" />
        <Label Grid.Row="1" Grid.Column="0" Content="Name" />
        <ComboBox Grid.Row="1" Grid.Column="1" />
    </Grid>
</UserControl>
